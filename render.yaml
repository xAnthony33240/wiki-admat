services:
  - type: web
    name: wiki-admat
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run server:prod
    envVars:
      - key: NODE_ENV
        value: production
